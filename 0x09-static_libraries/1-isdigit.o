#include "main.h"

/**
*_isdigit - checks whether character is numeric from 0 to 9
*@c: value of the int to be checked
*Return: if c is a digit 1,0 if otherwise
*/
int _isdigit(int c);
{
	return (c >= '0' && c <= '9');
}
